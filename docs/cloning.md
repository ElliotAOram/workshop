```bash
mkdir ~/github                                      # Create a new directory called github
cd ~/github                                         # Change directory so that your new directory is now your working directory 
git clone https://github.com/ElliotAOram/workshop   # Clone the workshop repository
cd workshop                                         # Change directory into the newly cloned local repository
```